# Dog Robot

This project implements a bidirectional communication system between an Arduino UNO and an ESP32 using NRF24L01 modules.

## Architecture

The project is split into two independent PlatformIO projects:

```txt
code/
├── arduino/
│   └── Arduino UNO code
│
└── esp32/
    └── ESP32 robot code
```
